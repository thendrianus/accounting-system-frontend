import { Component } from '@angular/core';

@Component({
  selector: 'selling',
  styleUrls: ['./selling.scss'],
  template: `<router-outlet></router-outlet>`
})
export class Selling {

  constructor(
  ) {
  }

}
